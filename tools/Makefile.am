bin_PROGRAMS = mtview

mtview_SOURCES = mtview.c
mtview_LDFLAGS = $(MTDEV_LIBS) $(EVEMU_LIBS) $(X11_LIBS) $(LIBM) $(CAIRO_LIBS)

AM_CFLAGS = $(MTDEV_CFLAGS) $(EVEMU_CFLAGS) $(X11_CFLAGS) $(CAIRO_CFLAGS)

if HAVE_XI22

AM_CFLAGS += $(XINPUT_CFLAGS)

mtview_LDFLAGS += $(XINPUT_LIBS)

endif
